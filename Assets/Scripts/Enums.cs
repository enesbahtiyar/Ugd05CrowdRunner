public enum BonusType
{
    Addition,
    Difference,
    Multiplication,
    Division
}

public enum GameState
{
    Menu,
    Game,
    LevelComplete,
    GameOver
}

public enum EnemyStates
{
    Idle,
    Running
}
